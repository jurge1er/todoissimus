# Kopiere diese Datei zu ".env" und trage deinen Todoist-Token ein
TODOIST_TOKEN=

# Optional: Port für den lokalen Server
#PORT=5173

# Optional: Lokales HTTPS aktivieren (für iOS PWA/HTTPS Tests)
#SSL_CERT_PATH=certs/localhost+1.pem
#SSL_KEY_PATH=certs/localhost+1-key.pem
#HTTPS_PORT=5443
