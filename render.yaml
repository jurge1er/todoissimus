services:
  - type: web
    name: todoissimus
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    autoDeploy: true
    envVars:
      # Set this in Render dashboard after creating the service.
      - key: TODOIST_TOKEN
        sync: false

